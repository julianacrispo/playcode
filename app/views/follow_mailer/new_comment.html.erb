<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>New comment to "<%= @product.name %>"</h1>
    <small>
      By: <%= @comment.user.name %>
    </small>
    <p>
      <%= @comment.body %>
    </p>
    <p>
      <%= link_to "View Comment on site", product_url(@product, @product, anchor: "comment-#{@comment.id}") %>
    </p>
  </body>
</html>