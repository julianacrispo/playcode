
<% if is_user? %>
  <% Vendor.all.each do |vendor| %>
    <% if @product.vendor_ids.include?(vendor.id) %>
    <%= link_to image_tag(vendor.image.url(:thumb)), vendor %>
    <% end %>
  <% end %>
<% else %>
    <% Vendor.take(2).each do |vendor| %>
      <% if @product.vendor_ids.include?(vendor.id) %>
        <%= link_to image_tag(vendor.image.url(:thumb)), vendor %>
      <% end %>
    <% end %>
<% end %>


