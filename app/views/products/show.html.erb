<div class="show-container"
  <div class="row">
    <div class="col-md-4">
      <div id="brand-head"> 
        <% if is_user? %>
          <%= render partial: 'follows/follow', locals: { product: @product } %>
        <% end %>
        <h4> Company </h4>
     </div>

        <%= image_tag @product.image.url(:medium) %>
        <p><%= @product.name %> </p>

        <% if is_admin? %>
          <%= link_to 'Edit', edit_product_path(@product) %>|
          <%= link_to 'Back', products_path %> |
          <%= link_to 'Delete', @product, method: :delete, data: { confirm: 'Are you sure?' } %>
            <% end %>
        </p>
      </div>


    <div class="col-md-4">
      <div id= "brand-head">
        <h4> Tech </h4>
   
      </div>
      <%= render 'vendor' %>
    </div> <!-- row end -->

    <div class= "col-md-4">
      <%= render partial: 'comments/pcomment' %>
    </div>
  </div>




